services:
  solquote:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - CSV_DIR=/app/data
    volumes:
      - ./data:/app/data
    ports:
      - "8080:8080"
    stop_grace_period: 15s
